[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[dependencies]
tonic = "0.12"
prost = "0.13"
tokio = { version = "1.37", features = ["macros", "rt-multi-thread"] }

[build-dependencies]
tonic-build = "*"
